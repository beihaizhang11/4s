# 打包说明文档

## 快速开始

### 方法1: 一键打包（推荐）

1. **双击运行打包脚本**
   ```
   双击 build.bat
   ```

2. **等待打包完成**
   - 脚本会自动检查依赖
   - 自动清理旧文件
   - 自动打包成exe
   - 打包完成后自动打开 `dist` 文件夹

3. **获取可执行文件**
   - 可执行文件位置: `dist\汽车维修服务账单生成器.exe`
   - 将整个 `dist` 文件夹复制到目标位置即可使用

### 方法2: 手动打包

如果你想手动控制打包过程：

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 清理旧文件
rmdir /s /q build dist
del *.spec

# 3. 打包
pyinstaller --name="汽车维修服务账单生成器" --windowed --onefile --hidden-import=win32timezone --hidden-import=openpyxl.cell._writer --collect-all=openpyxl main.py

# 4. 查看结果
cd dist
```

### 方法3: 使用高级配置文件

如果需要更多自定义选项：

```bash
# 使用spec文件打包
pyinstaller build_advanced.spec
```

## 打包模式

### 🎯 正式版本（默认）
- **脚本**: `build.bat`
- **特点**: 无控制台窗口，用户友好
- **用途**: 给最终用户使用

### 🐛 调试版本
- **脚本**: `build_debug.bat`
- **特点**: 有控制台窗口，显示错误信息
- **用途**: 调试问题时使用

## 打包参数说明

| 参数 | 说明 |
|------|------|
| `--name` | 可执行文件名称 |
| `--windowed` | 不显示控制台窗口（GUI程序） |
| `--onefile` | 打包成单个exe文件 |
| `--icon` | 指定程序图标（.ico文件） |
| `--add-data` | 添加额外的数据文件 |
| `--hidden-import` | 添加隐式导入的模块 |
| `--collect-all` | 收集某个包的所有文件 |
| `--noconfirm` | 覆盖输出目录时不询问 |

## 文件结构

打包后的文件结构：

```
dist/
├── 汽车维修服务账单生成器.exe   # 主程序
├── config.json                    # 配置文件（首次运行会生成）
└── output/                        # 输出文件夹（自动创建）
```

## 分发给用户

### 完整分发包

创建一个文件夹，包含以下内容：

```
汽车维修服务账单生成器/
├── 汽车维修服务账单生成器.exe
├── 使用说明.txt
└── Template - 汽车维修服务报价单 -/  # Excel模板文件夹
    ├── AUDI Brand Template.xlsx
    ├── VW Brand Template.xlsx
    └── VGIC Brand Template.xlsx
```

### 创建使用说明.txt

```txt
汽车维修服务账单生成器 - 使用说明

1. 首次运行配置:
   - 设置数据库路径（共享文件夹中的.db文件）
   - 设置输出文件夹
   - 设置教程附件路径
   - 点击"保存设置"

2. 添加模板:
   - 点击"添加模板"
   - 选择Excel模板文件
   - 配置字段映射
   - 配置邮件收件人

3. 生成账单:
   - 输入Task ID（支持多个，用逗号分隔）
   - 选择模板
   - 点击"生成账单并创建邮件"

技术支持: [你的联系方式]
```

## 常见问题

### Q: 打包失败，提示缺少模块？
A: 运行 `pip install -r requirements.txt` 安装所有依赖

### Q: 打包后的exe文件很大（超过100MB）？
A: 这是正常的，PyQt6和所有依赖都打包在里面了

### Q: 运行exe时出现错误？
A: 
1. 使用 `build_debug.bat` 打包调试版本
2. 运行调试版本查看控制台错误信息
3. 检查是否安装了Outlook

### Q: 如何添加程序图标？
A: 
1. 准备一个.ico文件（如logo.ico）
2. 修改build.bat中的 `--icon=NONE` 为 `--icon=logo.ico`
3. 重新打包

### Q: 如何减小exe文件大小？
A: 
1. 使用 `--exclude` 排除不需要的包
2. 编辑 `build_advanced.spec` 文件，在 `excludes` 列表中添加不需要的包
3. 使用 `pyinstaller build_advanced.spec` 打包

### Q: 打包后第一次运行很慢？
A: 这是正常的，PyInstaller会解压临时文件。后续运行会快很多。

### Q: 如何创建安装程序？
A: 可以使用以下工具创建安装包：
- Inno Setup (推荐，免费)
- NSIS
- Advanced Installer

## 优化建议

### 1. 添加程序图标
创建或下载一个.ico图标文件：
```bash
pyinstaller --icon=app_icon.ico ...其他参数...
```

### 2. 减小文件大小
使用UPX压缩（已在spec文件中启用）：
```bash
# UPX已在build_advanced.spec中启用
# upx=True
```

### 3. 分离文件模式
如果不需要单文件，可以用文件夹模式（启动更快）：
```bash
# 去掉 --onefile 参数
pyinstaller --name="..." --windowed main.py
```

## 技术细节

### 打包原理
PyInstaller会：
1. 分析Python脚本的依赖
2. 收集所有需要的模块和文件
3. 创建一个bootloader
4. 将所有内容打包成exe

### 隐式导入
某些模块需要显式声明：
- `win32timezone`: pywin32的时区支持
- `openpyxl.cell._writer`: Excel写入功能
- PyQt6相关模块

### 文件大小
- 基础大小: ~60MB (Python + PyQt6)
- openpyxl: ~5MB
- pywin32: ~10MB
- 总计: 约75-100MB

## 自动化打包脚本

如果需要版本控制和自动化：

```batch
@echo off
set VERSION=1.1.0
set BUILD_DATE=%date:~0,10%
set OUTPUT_NAME=汽车维修服务账单生成器_v%VERSION%_%BUILD_DATE%

pyinstaller --name="%OUTPUT_NAME%" --windowed --onefile main.py

echo 版本 %VERSION% 打包完成
echo 生成文件: dist\%OUTPUT_NAME%.exe
```

## 更新日志

### v1.1.0
- 添加批量处理功能
- 支持多个TO首位收件人
- 优化邮件正文格式

### v1.0.0
- 初始版本
- 基本的账单生成功能
- Outlook邮件集成

---

**祝打包顺利！** 🎉
